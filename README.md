# Lab 7
The final lab of CSE121. Involves using GET and POST requests using WiFi on the ESP32C3

## Usage
The WiFi should be configured in the sdkconfig file generated after `idf.py set-target <DEVICE_NAME>` is ran.
